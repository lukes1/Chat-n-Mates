services:
  - type: web
    name: chatwave
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 18
      - key: ENABLE_BOTS
        value: "false"
